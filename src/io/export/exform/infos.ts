// users/[uid]/infos.json
// pageslist // 这个是公用的，记录在meta
// scale ? // 临时数据，存本地或者数据库
// viewport ? // 临时数据，存本地或者数据库
// viewpageid ? // 临时数据，存本地或者数据库

import { Document } from "@/data/document";

// 记录临时数据
export function exportInfos(doc: Document) {

}